#include "csesbits.h"

int main(){
    fast;
    int n; cin>> n;
    vector<int> arr(n,0);
    for(int i = 0;i<n;i++) cin>>arr[i];

    int i = 0;
    unordered_set<int> s;
    unordered_set<int> final;
    while(i<arr.size()){
        if(s.count(arr[i])==0){
            s.insert((int)arr[i]);
        }
        else{
            s.clear();
            final.add
        }

    }



    return 0;
}